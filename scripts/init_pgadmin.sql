-- Run this once inside pgAdmin 4's Query Tool against your 'postgres'
-- maintenance database to create the faceid_db database. After it runs,
-- point your .env DATABASE_URL at it.

CREATE DATABASE faceid_db;
